import { NotFoundContent } from "@/components/layout/NotFoundContent";

/** Trade layout already renders shell + navbar — content only here. */
export default function TradeNotFound() {
  return <NotFoundContent />;
}
